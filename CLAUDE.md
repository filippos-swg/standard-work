# CLAUDE.md

This project runs on Project AIOS (v1.3).

Before any work: read `aios/AI_HANDOFF.md` and follow its read-first order.
Project truth lives in `aios/` and the repository — not in chat history.
Do not create AIOS core files at project root.
